<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <application
        android:name=".tuyaApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SPTM_systerm"
        tools:replace="android:allowBackup, android:supportsRtl"
        tools:targetApi="34">
        <activity
            android:name=".LoyalityPoints"
            android:exported="false" />
        <activity
            android:name=".powerMonitering"
            android:exported="false" />
        <activity
            android:name=".setThreshold"
            android:exported="false" />
        <activity
            android:name=".DeviceActivity"
            android:exported="false"
            android:theme="@style/Theme.SPTM_systerm" />
        <activity
            android:name=".RegisterUser"
            android:exported="false" />
        <activity
            android:name=".Home"
            android:exported="false" />

        <meta-data
            android:name="THING_SMART_APPKEY"
            android:value="hfeqthchgngnwp8stx8y" />
        <meta-data
            android:name="THING_SMART_SECRET"
            android:value="4wh8cn8pu45c5cx9rad8qt8mswj9dmwn" />

        <activity
            android:name=".SignUp"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <!-- Removed the MAIN action to avoid conflict -->
        </activity>
    </application>

</manifest>